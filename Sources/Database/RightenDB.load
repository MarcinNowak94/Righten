load database
     from sqlite:///Righten/Righten_mock.db
     into postgresql:///RightenDB

 with include drop, create tables, create indexes, reset sequences, quote identifiers

  set work_mem to '16MB', maintenance_work_mem to '512 MB';